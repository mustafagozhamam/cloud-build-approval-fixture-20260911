# Cloud Build approval fixture

Synthetic, non-sensitive fixture for verifying that a pending approval-bound
Cloud Build remains tied to the exact source and build configuration that
created it. It contains no production data, credentials, vulnerability details
or executable deployment logic.
